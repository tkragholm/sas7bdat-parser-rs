[workspace]
members = ["crates/sas7bdat-simd", "crates/profiler", "crates/polars_plugin"]
resolver = "2"

[profile.release]
lto = "thin"
codegen-units = 8
opt-level = 3
panic = "abort"

[profile.dist]
inherits = "release"
lto = "fat"
codegen-units = 1
strip = "symbols"
