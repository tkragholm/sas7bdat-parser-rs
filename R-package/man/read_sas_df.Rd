% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/readers.R
\name{read_sas_df}
\alias{read_sas_df}
\title{Read a SAS7BDAT file as a data frame or tibble}
\usage{
read_sas_df(
  path,
  as_tibble = TRUE,
  name_repair = "check_unique",
  convert_datetimes = TRUE,
  tz = "UTC",
  stringsAsFactors = FALSE
)
}
\arguments{
\item{path}{Path to a \code{.sas7bdat} file.}

\item{as_tibble}{If \code{TRUE} (default) and the \code{tibble} package is installed,
return a tibble; otherwise falls back to a base \code{data.frame}.}

\item{name_repair}{Passed to \code{tibble::as_tibble()} when \code{as_tibble = TRUE}.}

\item{convert_datetimes}{If \code{TRUE}, coerce columns labelled as \code{date},
\code{datetime}, or \code{time} into \code{Date}, \code{POSIXct} (UTC), or \code{difftime}/\code{hms}
objects respectively.}

\item{tz}{Time zone used when building \code{POSIXct} values for \code{datetime}
columns. Defaults to \code{"UTC"}.}

\item{stringsAsFactors}{Passed to \code{as.data.frame()} when \code{as_tibble = FALSE}.}
}
\value{
A tibble or \code{data.frame} with \code{column_types} and \code{row_count}
attributes preserved from the underlying Rust reader.
}
\description{
A lightweight R wrapper around the Rust-backed \code{\link[=read_sas]{read_sas()}} that returns an
R \code{data.frame} (or tibble when available) and applies basic temporal type
coercions based on the column metadata emitted by the parser.
}
